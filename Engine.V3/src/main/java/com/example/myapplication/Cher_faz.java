package com.example.myapplication;
/*Файл наладка - чередование фаз
 пока не используется, только активити, позже добавить вывод из БД состояние реле чередования фаз. если такое есть */
//
import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

public class Cher_faz extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_cher_faz);
    }
}
